# PostgreSQL connection string for the fritter_post database.
# Dev: point at a local or remote Postgres instance.
# Production: set in the Docker environment (see docker-compose.yml).
DATABASE_URL=postgresql://user:password@localhost:5432/fritter_post

# Node environment
NODE_ENV=development
